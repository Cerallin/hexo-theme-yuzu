| !{ partial('_partial/menu', null, {cache: true}) }

#index-content.content
  - for (const post of page.posts.toArray())
    | !{ partial('_partial/article-list-item', {post: post, condense: false}) }

  .pagination
    | !{ partial('_partial/pagination', {page: page}) }
