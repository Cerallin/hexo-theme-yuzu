.menu-nav {
  float: left;
  width: 19%;
  padding-top: 40px;
  color: secondary-trans;

  .menu-nav-item {
    font-size: 14px;

    .menu-nav-item-tit {
      padding-bottom: 5px;
      border-bottom: 1px solid #ddd;

      a {
        color: secondary-color;
        font-size: 16px;
        text-decoration: none;
      }
    }
  }

  div.menu-nav-item:not(:first-of-type) {
    margin-top: 30px;
  }
}

// Archive menu
.menu-nav-item-cont.archives {
  margin-top: 16px;

  .archive-list-item {
    height: 30px;
    line-height: 30px;

    a {
      color: primary-trans;
    }
  }
}

// Posts menu
.menu-nav-item-cont.posts {
  margin-top: 10px;

  .menu-nav-item {
    line-height: 24px;
    white-space: normal;
    padding-bottom: 5px;
    word-wrap: break-word;

    a {
      color: primary-trans;
    }
  }

  .menu-nav-item.empty {
    color: secondary-gray;
  }
}

// Tag menu
.menu-nav-item-cont.tags {
  margin-top: 16px;

  .tag-list-item {
    display: inline-block;
    padding: 0px 10px 7px 0px;

    a {
      color: primary-trans;
    }

    .tag-list-count {
      vertical-align: super;
      font-size: 0.6em;
      color: secondary-trans;
      margin-top: -0.5em;
    }
  }
}
